# 프로젝트 작업 지침

- 현재 에이전트는 설계·검토뿐 아니라 코드 구현, 파일 수정, 터미널 실행과 테스트를 직접 수행할 수 있다. Claude Code로 전달해야 한다는 역할 제한은 2026-09-16 해제됐다.
- 공통 협업 규칙은 `CLAUDE.md`를 읽고 따른다. 직접 확인한 사실, 실제 실행 결과, 추론을 구분한다.
- 기존 사용자 변경을 보존하며 기본 브랜치에서 직접 구현/커밋하지 않는다.
- 전체 데이터의 Phase 재학습은 별도 사용자 승인 후 실행한다. 단위 테스트와 축소 스모크 테스트는 가능하며 그 수치를 본 실험 결과로 인용하지 않는다.
- 기존 `PLAN.md`, `README.md`, `AUDIT.md`, `output/baseline_recovery.csv`, `output/baseline_recovery.md` 및 진단 시점 문서들은 별도 수정 요청 없이 변경하지 않는다. 정정과 구현 결과는 새 보고서에 남긴다.
