# ✈️ Flight Delay Prediction — Project Master Plan & Roadmap

> **저장소:** `https://github.com/Peter-jackson12/Airplane.git`  
> **환경:** Windows PowerShell, Python 3.14, `uv` 패키지 매니저, LightGBM, Pandas, Scikit-learn  
> **최종 갱신일:** 2026-09-14 (Phase 7-Ex 종료 및 코드 감사(`AUDIT.md`) 반영 시점)

> **⚠️ 2026-09-14 Phase 번호 체계 재정비**  
> 기존에 `run_phase7_weather_model.py`(기상 데이터 결합)와 로드맵상의 "Phase 7: 그랜드 슬램 융합 모델"이
> 동일한 번호를 점유하는 충돌이 있었다. 기상 실험은 성능 하락으로 폐기되었으므로 본선 번호를 부여하지 않고
> **`Phase 7-Ex` (폐기된 실험 브랜치)** 로 재명명하고, 이후 본선 계획을 한 칸씩 밀어 재번호 부여한다.
>
> | 구 번호 | 신 번호 | 항목 |
> | :--- | :--- | :--- |
> | Phase 7 (기상 실험) | **Phase 7-Ex** | 외부 기상 데이터 결합 — **폐기된 실험 브랜치** |
> | Phase 7 (로드맵) | **Phase 8** | 그랜드 슬램 융합 모델 |
> | Phase 8 (로드맵) | **Phase 9** | CatBoost / XGBoost 이종 앙상블 |
> | Phase 9 (로드맵) | **Phase 10** | 모델 경량화 및 추론 서빙 (MLOps) |

---

## 1. 프로젝트 개요 및 핵심 데이터셋 명세

* **문제 정의:** 미국 교통통계국(BTS) 항공 운항 데이터를 기반으로 항공편의 15분 이상 도착 지연(`Delay`) 여부를 사전 예측 (불균형 이진 분류)
* **평가 지표:** **Macro F1-Score**, **Log Loss** (보조: **ROC-AUC**)
* **데이터 규모 (100만 행):**
  * 원본 데이터: `data/train.csv` (1,000,000행 × 19열, 약 111.3MB, `.gitignore` 처리 완료)
  * 라벨 데이터(학습/검증용): **255,001행 (25.5%)**
    * `Not_Delayed` (0): 210,001건 (82.35%)
    * `Delayed` (1): 45,000건 (17.65%) ➔ **클래스 불균형 약 4.67 : 1**
  * 미라벨 데이터(준지도 학습용): **744,999행 (74.5%)** (`Delay` 결측치)

---

## 2. 실험 및 성능 추이 (Phase 1 ~ Phase 7-Ex, 이후 계획)

모든 평가는 라벨 데이터 255,001건에 대해 **Stratified 5-Fold OOF (Out-of-Fold)**로 측정되었다.

> **⚠️ 아래 수치 해석 시 필수 주의 (근거: [AUDIT.md](AUDIT.md))**
> 1. **Phase 5는 fold 경계를 넘는 데이터 누수가 확인되었다.** 수치는 낙관 편향을 포함하며 재측정 전까지 확정치가 아니다 (§3-6).
> 2. **Phase 간 평가 프로토콜이 서로 달라 순차 비교는 통계적으로 무효다.** 각 단계에서 피처·하이퍼파라미터·임계값 탐색 범위가 동시에 변경되었다 (§3-9).
> 3. 따라서 아래 표는 **"각 스크립트를 그 스크립트 자신의 설정으로 돌렸을 때의 실행 기록"**이며, 기법 간 우열의 근거로는 사용할 수 없다.

### 2-1. 실측 완료 단계

| 실험 단계 | 스크립트 파일명 | 핵심 엔지니어링 기법 | 최적 임계값 | Log Loss | ROC-AUC | Macro F1 | 지연 감지(TP) | Recall | 검증 상태 |
| :--- | :--- | :--- | :---: | :---: | :---: | :---: | :---: | :---: | :--- |
| **Phase 1** | `run_baseline.py` | 기본 전처리 + Label Encoding | 0.50 | 0.4636 | 0.6366 | 0.4516 | 0건 | 0.00% | ⚠️ 지표를 fold 평균으로 집계 (타 단계는 pooled OOF) |
| **Phase 2** | `run_tuned.py` | 노이즈 Pruning + Threshold Search | **0.22** | 0.4635 | 0.6357 | **0.5724** | 13,039건 | 28.98% | ⚠️ docstring의 "OOF TE"는 **미구현** (§3-7) |
| **Phase 3** | `run_target_encoded.py` | 카테고리 삭제 후 OOF TE 단독 치환 | 0.22 | 0.4635 | 0.6043 | 0.5521 | 14,895건 | 33.10% | ✅ TE 누수 없음 |
| **Phase 4** | `run_hybrid.py` | Category Dtype + OOF TE 동시 투입 | 0.22 | 0.4633 | 0.6107 | 0.5579 | 14,386건 | 31.97% | ✅ TE 누수 없음 |
| **Phase 5** | `run_pseudo_labeling.py` | 8.9만 건 증강 준지도 학습 | 0.24 | 0.4587 | 0.6416 | 0.5746 | 13,527건 | 30.06% | **❌ 누수 확정 — 재측정 필요 (§3-6)** |
| **Phase 6** | `run_advanced_features.py` | **시간 결측 역산 + 공항 트래픽** | **0.21** | 0.4629 | 0.6345 | 0.5721 | **15,866건** | **35.26%** | ✅ 타깃 누수 없음 (Traffic 피처 포함, §3-8) |
| **Phase 7-Ex** | `run_phase7_weather_model.py` | 외부 기상 데이터 10종 결합 | 0.21 | 0.4634 | 0.5964 | 0.5465 | 16,857건 | 37.46% | **🗑️ 폐기 — 원천 데이터 결함 (§3-12)** |

**Phase 5 수치에 대한 보정 추정** — 누수 제거 시 예상 하락폭은 **ROC-AUC −0.002 ~ −0.008, Macro F1 −0.002 ~ −0.006**이다(`AUDIT.md` §2.1-C). 이 폭을 적용하면 Phase 5의 AUC 0.6416은 **0.634 ~ 0.640** 구간으로, Phase 4(0.6107)보다는 여전히 높지만 Phase 6(0.6345)과는 **구분되지 않는 수준**이 된다. 즉 "전 지표 역대 최고치"라는 기존 서술은 재측정 전까지 보류한다.

### 2-2. 계획 단계 (미실행 — 아래 수치는 실측치가 아닌 **목표치**)

| 실험 단계 | 스크립트 파일명 | 핵심 기법 | Log Loss 목표 | ROC-AUC 목표 | Macro F1 목표 | 상태 |
| :--- | :--- | :--- | :---: | :---: | :---: | :--- |
| **Phase 8** | `run_grand_slam.py` | 누수 제거 준지도 증강 + Phase 6 도메인 피처 | ≤ 0.4600 | ≥ 0.6400 | ≥ 0.5800 | 🔒 선행 과제 완료 후 착수 (§4-0) / **v0 시제품 폐기됨 — 아래 주석 참조** |
| **Phase 9** | `run_ensemble.py` | CatBoost / XGBoost 이종 Soft-Voting | ≤ 0.4570 | ≥ 0.6500 | ≥ 0.5850 | ⬜ 미착수 |
| **Phase 10** | `serve_pipeline.py` | Feature Pipeline 직렬화 및 추론 서빙 | (성능 동등 유지) | (동등) | (동등) | ⬜ 미착수 |

> **Phase 8 v0 시제품 처리 (2026-09-15):** `run_grand_slam_v0_leaky.py`(구 `run_grand_slam.py`)로 시제품이 존재하나, §3-6 누수를 그대로 상속하여 **폐기하고 수치는 기록하지 않는다.** 산출물 차트는 `output/_deprecated/`로 격리했다. 상속 경로는 원본과 동일하다 — teacher가 5-fold 앙상블 평균(`:238`)이라 전체 라벨을 소비하고, 분위수·pseudo 집합이 fold 루프 밖에서 동결된 뒤(`:246-258`) 모든 fold에 주입된다(`:276`). 파일명의 `_leaky` 접미사는 이 스크립트의 OOF 수치를 인용하지 말라는 표식이며, Phase 8 본선은 `run_grand_slam.py`로 새로 작성한다.

> Phase 8의 목표 LogLoss를 기존 계획의 0.455대에서 **0.4600 이하**로 상향 조정한 이유: 기존 목표는 누수가 포함된 Phase 5의 0.4587을 기준선으로 삼아 설정된 값이었다. 정직한 기준선이 확정되기 전에는 그보다 공격적인 목표를 유지할 근거가 없다.

---

## 3. 핵심 도메인 규칙 및 검증된 트러블슈팅 인사이트

### 3-A. 모델링 도메인 규칙 (Phase 1 ~ 6에서 확립)

1. **임계값과 베이스 레이트 연동:** 자연 지연율이 17.65%이므로 기본값 0.50은 무조건 실패함. 최적 임계값은 항상 **`0.21 ~ 0.24`** 구간에서 형성됨.
2. **하이브리드 카테고리 보존:** 고카디널리티 변수(`Tail_Number`, `Route` 등)는 Target Encoding만 쓰면 안 되며, **원본 판다스 `category` 컬럼과 `TE_` 수치 컬럼을 반드시 함께 투입**해야 함.
3. **안전한 준지도 학습(Pseudo-Labeling) 선별 기준:** 단순 백분위수 15:15 선별은 정상 확률 78%짜리를 지연으로 속이는 데이터 오염을 유발함. **정상은 하위 10%($p \le 0.184$), 지연은 상위 2%($p \ge 0.235$)만 엄선**하여 89,438건을 증강함. — ⚠️ 단, 이 선별로 얻은 성능 상승분은 §3-6의 누수와 분리되지 않았으므로 **"성능이 상승함"은 아직 검증되지 않은 주장**이다.
4. **시간 결측 역산 공식:** 출발/도착 중 한쪽만 있는 단측 결측치(10.9%)는 동일 노선(`Route`)의 중앙값 소요시간(`median_duration`)으로 역산($Arr = Dep + Dur$)하여 `Estimated_Duration` 결측치를 0%로 제거함.
5. **판다스 `category` 병합 주의:** `pd.concat` 후 카테고리 목록 불일치로 `object`로 풀리는 버그 방지를 위해 병합 후 `df[col] = df[col].astype("category")` 명시적 재선언 필수.

### 3-B. 코드 감사에서 확정된 검증 인사이트 (2026-09-14, [AUDIT.md](AUDIT.md))

6. **[★ 최우선] 준지도 학습의 fold 경계 누수 — Phase 5 수치는 낙관 편향을 포함한다.**
   * **현상:** `run_pseudo_labeling.py:201`의 teacher 예측은 **5개 fold 모델의 평균**이다. 따라서 임의의 라벨 행은 5개 teacher 중 4개의 학습셋에 포함되며, `unlabeled_preds`는 라벨 255,001건 **전부**의 함수가 된다.
   * **전파 경로:** 그렇게 만들어진 `pseudo_X`/`pseudo_y`가 fold 루프 **밖에서 동결**된 뒤(`:210-228`) 모든 fold의 학습셋에 동일하게 주입된다(`:248`). 가장 직접적인 오염 경로는 `:257-261`로, 오염된 `y_tr_aug`로 계산한 TE 값이 **검증 행의 피처에 그대로 부착**된다.
   * **규모:** AUC **+0.002 ~ +0.008**, Macro F1 **+0.002 ~ +0.006** (중앙 추정 AUC +0.004). 누수가 이 수준에 머무는 이유는 두 가지 억제 요인이 확인되었기 때문이다 — 미라벨↔라벨 **완전 중복 항공편이 0.003%(20건)뿐**이라 행 단위 암기가 불가능하고, `Day_of_Month`가 제거되어 "같은 날 같은 기체"(13.45%) 지연 전파 경로가 닫혀 있다.
   * **규칙:** **teacher는 반드시 fold 루프 안에서, 해당 fold의 학습 데이터만으로 적합해야 한다.** 분위수 임계값(`np.percentile`)도 fold별로 재계산해야 한다. 이 구조를 지키지 않은 준지도 결과는 OOF 수치로 보고하지 않는다.
7. **OOF Target Encoding의 fold 위치는 전 스크립트에서 정상 — 단 `run_tuned.py`는 TE 자체가 없다.**
   * TE를 사용하는 5개 스크립트(Phase 3·4·5·6·7-Ex) 모두 TE를 fold 루프 **내부**에서 계산한다. 이 부분은 누수가 없다.
   * 그러나 `run_tuned.py`는 docstring이 "OOF Target Encoding (Tail_Number, Route)"을 선언하는데 **파일 전체에 TE 코드가 존재하지 않는다.** Phase 2의 실제 개선 내용은 (a) 컬럼 Pruning, (b) 하이퍼파라미터 확대, (c) 임계값 최적화 도입 세 가지뿐이다.
   * **교훈:** Phase 2→3의 성능 변화를 "TE 도입 효과"로 해석해서는 안 된다. Phase 3은 TE 도입과 **원본 범주 컬럼 삭제**를 동시에 수행했으므로 두 효과가 뒤섞여 있다.
8. **Traffic(혼잡도) 피처는 전체 데이터 기준 집계이지만 타깃 누수가 아니다.**
   * 집계 대상이 `Route`의 `count`이고 `Delay`가 `groupby` 키·집계 대상·필터 어디에도 등장하지 않는다. 라벨에서 이 피처로 향하는 정보 경로가 코드상 존재하지 않는다.
   * 형식적으로는 transductive 집계이나, **운항 스케줄은 예측 시점에 이미 확정·공개된 정보**이므로 배포 환경에서 동일하게 재현 가능하다. 낙관 편향 0으로 판정한다.
   * **단, 실제 결함이 하나 있다:** `Dep_Hour == -1`(시각 결측) 행들이 하나의 거대한 버킷으로 묶여 "혼잡도"가 아니라 **"결측 밀도"**를 측정한다. 집계 시 결측 시각 행을 제외해야 한다.
9. **평가 프로토콜 불일치로 Phase 간 성능 비교는 무효다.**
   * 7개 스크립트가 공유하는 설정은 `n_splits=5`, `shuffle=True`, `random_state=42` **세 가지뿐**이다.
   * 임계값 탐색 범위가 **`0.10~0.69`(60점, Phase 2·3) / `0.15~0.49`(35점, Phase 4~7-Ex) / 탐색 없음(Phase 1)** 로 갈린다. 협역 그리드는 0.50 이상을 탐색하지 못하고, 광역 그리드는 winner's curse를 더 많이 얻는다.
   * **Phase 1만 지표를 fold 평균으로 집계**하고 나머지는 pooled OOF로 집계한다. Macro F1은 두 방식의 값이 서로 다르므로 **같은 표에 나란히 놓으면 안 되는 양**이다.
   * Phase 5→6 사이에는 변경 요인이 10개 이상(Traffic 피처, 시각 역산, Sin/Cos 결측 처리, 하이퍼파라미터 6종, TE 평활 `m` 20→25, pseudo-label 제거)이다.
   * **규칙:** 앞으로 Phase를 비교할 때는 **하이퍼파라미터와 프로토콜을 고정한 채 피처셋만 변경**한다. 공통 모듈 `src/features.py` + 단일 `CVConfig`로 강제한다(§4-0).
10. **전 스크립트 공통 — 채점 fold를 early stopping에 사용하고, 임계값을 채점 대상에서 고른다.**
    * 7개 전부 `eval_set=[(X_val, y_val)]` + `early_stopping`을 쓴 뒤 **같은 `X_val`로 OOF 점수를 매긴다.** 트리 개수가 채점 데이터에서 선택되는 셈이다 (AUC **+0.000~+0.002**).
    * 6개 스크립트가 255,001행 OOF 벡터에서 35~60개 임계값을 훑어 **최댓값**을 성능으로 보고한다 (winner's curse, Macro F1 **+0.001~+0.002**).
    * 절대값은 작지만 **보고되는 Phase 간 개선 폭 자체가 같은 자릿수**다. 0.002 이하의 차이는 실질 개선으로 해석하지 않는다.
11. **`Cancelled` / `Diverted`는 "누수 컬럼"이 아니라 상수 0 컬럼이다.**
    * 100만 행 전수 확인 결과 두 컬럼 모두 **전 행이 0**이다(분산 0). 제거하는 것은 옳지만 근거는 "누수 방지"가 아니라 "무정보"다.
    * 부수 효과로, Traffic 집계에 결항편이 섞이는 문제는 실제로 존재하지 않는다.
12. **Phase 7-Ex 실패의 1차 원인은 모델이 아니라 원천 데이터 결함이다.**
    * `train_with_weather.csv` 100만 행 전수 확인: **`Weather_Origin_snow`(적설)와 `Weather_Origin_wpgt`(돌풍)는 유효값이 단 한 건도 없다(100% 결측).** 스크립트 docstring이 내세운 "풍속, 돌풍, 적설, 강수" 중 절반이 애초에 수집되지 않았다.
    * 나머지도 결합률이 **55.77%**(라벨 행 기준 55.17%)에 그친다. 미국 30대 허브 공항만 매핑했고 `Dep_Hour == -1` 행은 영구 미매칭이기 때문이다.
    * **도착지 기상이 아예 없다.** `merge_weather_pipeline.py:136-142`는 `Origin_Airport` 한쪽만 조인한다(컬럼 접두어가 전부 `Weather_Origin_`).
    * `TARGET_YEAR = 2022` 가정을 **프로그램적으로 검증하는 코드가 저장소에 없다.** `find_flight_year.py`는 구글 검색 링크를 출력하는 수동 보조 도구일 뿐이다.
    * **교훈:** 외부 데이터 결합 실패를 모델 측 원인(시차, `colsample_bytree` 희석)으로 진단하기 **전에**, 결합 산출물의 컬럼별 유효값 비율을 먼저 확인해야 한다. 전량 결측 컬럼의 피처 중요도가 0인 것은 "트리가 배제한" 결과가 아니라 **분기할 값이 없는 기계적 귀결**이다.

---

## 4. 향후 로드맵 (2026-09-14 기준 재작성)

현재 상태: **Phase 1~6 실행 완료, Phase 7-Ex 폐기 완료, 코드 감사 완료.** 감사 결과 Phase 5에 누수가 확인되었고 Phase 간 비교 프로토콜이 불일치하므로, **신규 Phase 착수보다 기준선 복구가 선행되어야 한다.** Phase 8(그랜드 슬램)은 정의상 Phase 5의 준지도 증강을 그대로 물려받으므로, 지금 착수하면 누수를 그대로 상속하게 된다.

### 4-0. 선행 과제 — 기준선 복구 (Phase 번호 미부여, Phase 8의 차단 조건)

* [ ] **P0-1. 공통 모듈 `src/features.py` 신설**
  * 7개 스크립트에 7벌씩 복사된 전처리·피처엔지니어링을 단일 모듈로 통합 (`AUDIT.md` §1의 시그니처 설계안 적용).
  * `oof_target_encode()`가 `outer_train_idx` / `outer_valid_idx`를 **필수 인자**로 요구하게 하여, fold 밖 TE 계산을 시그니처 레벨에서 차단.
  * `make_pseudo_labels()`가 "fold 전용 teacher 예측 함수"를 받게 하여 §3-6 누수를 구조적으로 차단.
* [ ] **P0-2. `CVConfig` 단일화 및 전 Phase 재실행**
  * `n_splits` / `seed` / `stopping_rounds` / 임계값 그리드 / 지표 집계 방식을 한 세트로 고정하고, **하이퍼파라미터를 고정한 채 피처셋만** Phase별로 변경하여 Phase 1~6을 전부 재측정.
  * 산출물: 프로토콜이 통일된 새 성능 추이 표. 이것이 확정되기 전까지 §2-1의 수치는 잠정치로 취급.
* [ ] **P0-3. Phase 5 정직한 재측정**
  * teacher를 fold 루프 안으로 이동하여 재실행하고, 현행 수치와 **병기**한다. 그 차이가 §3-6 추정치(AUC +0.002~0.008)의 실측값이 된다.
* [ ] **P0-4. 평가 편향 제거**
  * early stopping을 학습 fold 내부 holdout으로 이전 (§3-10).
  * 임계값 선택을 nested 방식으로 전환 — K-1 fold에서 고르고 남은 fold에서 채점.
* [ ] **P0-5. 문서·주석 정정**
  * `run_tuned.py` docstring의 미구현 "OOF Target Encoding" 서술 제거 (§3-7).
  * `Cancelled`/`Diverted`의 "누수 컬럼" 주석을 "상수 0 무정보 컬럼"으로 정정 (§3-11).

### 4-1. Phase 8: 그랜드 슬램 융합 모델 (`run_grand_slam.py`) 🔒

> **차단 조건: P0-1 ~ P0-4 완료.** 누수가 제거된 준지도 파이프라인이 확보되기 전에는 착수하지 않는다.

* [ ] **누수 제거 준지도 증강**(fold 내부 teacher) + **Phase 6 도메인 피처**(시간 결측 역산 + 공항 트래픽)를 단일 파이프라인으로 결합.
* [ ] Traffic 집계에서 `Dep_Hour == -1` 버킷 분리 (§3-8).
* [ ] TE에 inner K-fold 적용 — 학습 행이 자기 라벨로 인코딩되는 문제 해소.
* **목표:** LogLoss ≤ 0.4600, ROC-AUC ≥ 0.6400, Macro F1 ≥ 0.5800 (**정직한 기준선 대비** 개선폭으로 판정하며, 0.002 이하 차이는 개선으로 인정하지 않음, §3-10)
* **성공 판정 기준:** P0-2에서 확정된 Phase 6 재측정치를 기준선으로 삼아, 그보다 AUC +0.005 이상 상승할 때만 채택.

### 4-2. Phase 9: CatBoost / XGBoost 이종 앙상블 (`run_ensemble.py`)

* [ ] 고카디널리티(`Tail_Number` 6,430 / `Route`)에 특화된 **CatBoost Ordered Target Statistics** 교차 검증.
  * CatBoost의 Ordered TS는 §3-6·§3-7에서 지적한 TE 누수 문제를 알고리즘 차원에서 회피하므로, 수동 TE 구현을 대체할 후보로서도 가치가 있다.
* [ ] LightGBM(Phase 8) + CatBoost 5:5 Soft-Voting, 이후 가중치 탐색.
* [ ] **앙상블 가중치는 반드시 nested CV로 결정한다** — OOF 벡터에서 직접 가중치를 고르면 §3-10과 동일한 winner's curse가 발생한다.
* **목표:** LogLoss ≤ 0.4570, ROC-AUC ≥ 0.6500, Macro F1 ≥ 0.5850

### 4-3. Phase 10: 모델 경량화 및 추론 서빙 파이프라인 (`serve_pipeline.py`, MLOps)

* [ ] `src/features.py` 기반 Feature Pipeline 직렬화 (`joblib` / `onnx`).
* [ ] 실시간 추론용 단일 진입점 작성 — 학습 시점 TE 매핑 테이블과 `LabelEncoder` vocabulary를 **함께** 직렬화 (미처리 시 신규 범주에서 즉시 장애).
* [ ] 배포 시점 재현 가능성 검증: Traffic 피처는 운항 스케줄만으로 계산 가능함을 확인하고(§3-8), 학습 시점 통계에 의존하는 피처가 없는지 점검.
* [ ] 임계값을 설정값으로 외부화 (운영 중 재조정 가능하도록).

### 4-4. 조건부 재개 항목 — 기상 데이터 (Phase 7-Ex 재도전)

> 현재 **보류**. 아래 4개 조건이 모두 충족될 때만 재개한다 (§3-12).

* [ ] `snow` / `wpgt` 전량 결측 해소, 또는 해당 변수 공식 포기 선언.
* [ ] 도착지 기상(`Weather_Dest_*`) 추가 결합.
* [ ] `TARGET_YEAR = 2022` 가정의 프로그램적 검증 (BTS 원본 대조 등).
* [ ] UTC ↔ 공항 현지 시각 변환 적용 후 결합률 재측정 (현행 55.77%).

---

## 5. 문서 간 정합성 기준

* **성능 수치의 단일 진원지(Single Source of Truth)는 본 문서 §2-1이다.** `README.md` §5-1의 표는 본 표와 동일한 값을 유지해야 하며, 재측정 시 두 문서를 함께 갱신한다.
* **Phase 번호는 본 문서 상단의 재정비 표를 따른다.** `README.md`, 스크립트 docstring, 커밋 메시지 모두 `Phase 7-Ex` / `Phase 8` / `Phase 9` / `Phase 10` 표기를 사용한다.
* **누수·편향 관련 판정의 진원지는 [AUDIT.md](AUDIT.md)이다.** 본 문서 §3-B는 그 요약이며, 상세 근거(코드 라인 인용, 측정 절차)는 원본을 참조한다.
