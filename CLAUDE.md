# CLAUDE.md — 프로젝트 협업 지침

이 파일은 Claude Code(VS Code 확장/터미널 에이전트)가 이 저장소에서 작업할 때
따르는 지침이다. `PLAN.md`/`README.md`/`AUDIT.md`는 프로젝트 산출물이며, 이
파일은 그 위에서 "누가 무엇을 담당하는가"와 "어떻게 보고하는가"를 정의한다.

## 1. 역할 분담

사용자는 2026-09-16 역할 제한을 해제했다. 현재 작업을 맡은 대화 모델/Codex와
VS Code Claude Code Agent 모두 설계, 코드 검토·구현, 파일 수정, 터미널 실행,
테스트를 직접 수행할 수 있다. 구현을 반드시 다른 에이전트에 전달할 필요는 없다.
서로 같은 파일을 동시에 수정하지 않으며 기존 사용자 변경을 보존한다.
이 권한 변경은 전체 Phase 재학습 승인(§3)이나 commit/push 승인을 대신하지 않는다.

작업지시서에 없는 범위(예: 읽기 전용 감사 지시에서 코드 수정, 커밋/푸시)로
스스로 확장하지 않는다. 범위가 모호하면 실행 전에 확인을 구한다.

## 2. 보고 시 사실/추론 구분

보고서·커밋 메시지·대화 응답 어디에서든 다음 세 범주를 섞지 않는다.

1. **직접 확인한 사실** — 코드를 읽거나(`Read`/`Grep`) 파일을 직접 열어 확인한
   내용. "N행에서 X를 확인했다"처럼 근거(파일:행)를 남긴다.
2. **실행 보고** — 명령을 실제로 실행해 나온 출력(테스트 결과, 로그, 수치).
   실행하지 않은 결과를 실행한 것처럼 서술하지 않는다.
3. **추론** — 위 두 가지로부터 유도한 판단·예측·설계 권고. "~로 보인다",
   "~일 가능성이 있다"처럼 확정 사실과 구분되는 표현을 쓴다.

이 구분은 `output/es_diagnosis.md`, `output/es_protocol_final.md`,
`output/audit_addendum_te_leak.md`, `output/nested_grid_implementation.md`가
이미 실천해 온 관례이며, 이후 모든 감사/구현 보고서에도 동일하게 적용한다.

## 3. 전체 Phase 재실행 승인 게이트

전체 데이터로 Phase 재학습을 실행하기 전에는 별도 사용자 승인을 받는다.
읽기, 구현, 단위 테스트, 축소 스모크 테스트는 현재 작업 범위 안에서 직접 진행한다.
스모크 결과를 본 실험의 성능으로 인용하지 않는다.

기존 진단 문서와 `output/baseline_recovery.csv`/`.md`는 보존한다.
새 실행은 `baseline_recovery_v2` 계열 경로와 스키마/실험 지문 검사로 분리한다.
이름이 같아도 데이터·코드·설정이 다르면 새 출력 이름이 필요하다.
`--force`는 프로토콜 검증을 우회하는 옵션이 아니다.
