# 머신러닝 기반 항공편 운항 지연(Flight Delay) 예측 프로젝트

## 0. 표지
* **프로젝트 명:** 머신러닝 기반 항공편 운항 지연(Flight Delay) 사전 예측 모델 개발
* **작성자:** Peter-jackson12 TF
* **작성일:** 2026년 09월 14일
* **버전:** v0.6 (Phase 1 Baseline부터 Phase 7 외부 기상 데이터 결합 및 사후 부검(Post-Mortem)까지 반영)

---

## 1. 목적 및 개요
* **최종 목적:** 항공기 스케줄, 운항 계획 데이터를 기반으로 출발 전 운항 지연(Delay) 발생 여부를 사전에 예측하여 비정상 운항 대응 지원.
* **예측 또는 분류 대상:** 개별 항공편의 도착 지연 여부 (`Delay`: `Delayed` vs `Not_Delayed`, 이진 분류)
* **기대 효과:** 선제적 지연 예측을 통한 공항 주기장 및 지상 조업 자원 최적화, 승객 보상 및 연결편 실패 비용 절감.

### 1-1. 추진 배경
* **추진 배경:** 항공 네트워크의 연쇄 지연(Reactionary Delay) 특성상 사전 지연 탐지가 필수적임.
* **기존 방식의 한계:** 사후 대응 중심 및 단순 기상 특보 의존으로 네트워크 혼잡도 및 기체 회전 지연 반영 한계.
* **문제 정의:** 항공 스케줄, 노선, 기체, 항공사 정보를 활용하여 예정된 항공편의 운항 지연 여부(`Delayed`)를 사전 판별한다.
* **문제 유형:** 불균형 이진 분류 (Binary Classification) 및 준지도 학습(Semi-Supervised Learning)
* **분석 단위:** 개별 항공편 1회 운항 레그(Flight Leg ID)
* **주요 평가 지표:** Macro F1-Score, Log Loss (보조 지표: ROC-AUC)

---

## 2. 데이터 구조 파악
* **데이터 구성:** 18개 설명 변수 + 1개 타깃 변수(`Delay`) (+ Phase 7 외부 기상 피처 10개 실험)
* **데이터 출처:** 미국 교통통계국(BTS) 항공 운항 정시성 데이터 가공셋
* **분석 범위:** 운항 계획 항공편 (결항/회항 제외)

### 2-1. 데이터 개요
* **전체 행 수:** 1,000,000 행
* **라벨 데이터 수:** 255,001 행 (25.5%)
* **미라벨 데이터 수:** 744,999 행 (74.5%)
* **타깃 클래스 분포 (라벨 기준):**
  * `Not_Delayed` (0): 210,001 행 (82.35%)
  * `Delayed` (1): 45,000 행 (17.65%)
  * 불균형 비율: 약 4.67 : 1

### 2-2. 테이블 정의서

| 컬럼명 | 데이터 타입 | 설명 | 결측 비율 | 비고 |
| :--- | :---: | :--- | :---: | :--- |
| `ID` | String | 고유 식별자 | 0.0% | 모델링 시 제거 |
| `Month` | Integer | 운항 월 (1~12) | 0.0% | 기본 제공 |
| `Day_of_Month` | Integer | 운항 일 (1~31) | 0.0% | 기본 제공 |
| `Estimated_Departure_Time` | Float/Int | 예정 출발 시각 (HHMM) | 약 10.9% | 시간 파싱 및 역산 복원 |
| `Estimated_Arrival_Time` | Float/Int | 예정 도착 시각 (HHMM) | 약 10.9% | 시간 파싱 및 역산 복원 |
| `Cancelled` | Integer | 결항 여부 (0 또는 1) | 0.0% | 모델링 시 제거 |
| `Diverted` | Integer | 회항 여부 (0 또는 1) | 0.0% | 모델링 시 제거 |
| `Origin_Airport` | String | 출발 공항 IATA 코드 | 0.0% | 카테고리 인코딩 |
| `Origin_Airport_ID` | Integer | 출발 공항 식별 번호 | 0.0% | 모델링 시 제거 |
| `Origin_State` | String | 출발 공항 소재 주 | 일부 결측 | 교차 대치 후 Pruning |
| `Destination_Airport` | String | 도착 공항 IATA 코드 | 0.0% | 카테고리 인코딩 |
| `Destination_Airport_ID` | Integer | 도착 공항 식별 번호 | 0.0% | 모델링 시 제거 |
| `Destination_State` | String | 도착 공항 소재 주 | 일부 결측 | 교차 대치 후 Pruning |
| `Distance` | Float | 운항 거리 (Miles) | 0.0% | 수치형 |
| `Airline` | String | 운항 항공사 명칭 | 약 10.6% | 교차 대치 복원 |
| `Carrier_Code(IATA)` | String | 항공사 IATA 코드 | 약 10.8% | 교차 대치 복원 |
| `Carrier_ID(DOT)` | Float/Int | 항공사 교통부 ID | 약 10.9% | 모델링 시 제거 |
| `Tail_Number` | String | 항공기 기체 번호 | 0.0% | 고카디널리티 범주형 |
| `Delay` | String | 지연 여부 (Target) | 74.5% (미라벨) | 이진 분류 타깃 |

---

## 3. 데이터 전처리 및 피처 엔지니어링

### 3-1. 핵심 전처리 및 도메인 피처링 기법
1. **결측치 교차 대치:**
   * `Carrier_Code(IATA)` $\leftrightarrow$ `Airline` 상호 매핑 대치
   * `Origin_Airport` $\leftrightarrow$ `Origin_State` 상호 매핑 대치
2. **시간 결측치 역산 복원 (Phase 6 반영):**
   * 출발 또는 도착 시각 중 한쪽만 존재하는 단측 결측치(약 10.9%)에 대해, 동일 노선(`Route`)의 **비행 소요시간 중앙값(Median Duration)**을 계산하여 결측 시각을 물리적으로 역산 복원.
   * 비행 소요시간(`Estimated_Duration`) 및 순항 속도(`Air_Speed_Proxy`) 결측치를 0%로 완전 제거.
3. **공항별 시간대별 트래픽 혼잡도(Congestion) 피처:**
   * `(Month, Day_of_Month, Origin_Airport, Dep_Hour)` 단위 동시간대 출발 항공편 총수를 집계하여 `Origin_Traffic` 생성.
   * 도착 공항 활주로/게이트 병목을 측정하는 `Dest_Traffic` 생성.
4. **24시간 주기성 및 야간 보정:**
   * 자정을 넘기는 야간 비행($Arr < Dep \implies +1440분$) 계산 보정.
   * 시간의 연속성을 위한 `Sin_Dep_Hour`, `Cos_Dep_Hour` 삼각함수 순환 피처 생성.
5. **누수 방지 피처 제거:**
   * `ID`, `Cancelled`, `Diverted`, `Origin_Airport_ID`, `Destination_Airport_ID`, `Carrier_ID(DOT)` 제거.

### 3-2. 파이프라인 고도화 로드맵

```text
================================================================================================
                      [항공편 지연 예측 파이프라인 고도화 로드맵]
================================================================================================

  [Phase 1: Baseline] run_baseline.py
  ┌───────────────────────┐
  │ 원본 100만 행 로드    │ ──▶ 공항/항공사 결측 상호 복원 & 시간대 추출
  └───────────────────────┘
            │
            ▼ (Label Encoding + 기본 임계값 0.50)
  ┌────────────────────────────────────────────────────────────────────────┐
  │ 결과: LogLoss 0.4636 | ROC-AUC 0.6366 | Macro F1 0.4516                │
  │ 한계: 불균형(17.6%)으로 인해 모델이 모든 비행기를 100% 정상(0)으로 분류 │
  └────────────────────────────────────────────────────────────────────────┘
            │
            ▼
  [Phase 2: Threshold Tuning] run_tuned.py
  ┌───────────────────────┐
  │ 노이즈 피처 Pruning   │ ──▶ 중요도 0인 State, Minute 등 과적합 유발 변수 삭제
  └───────────────────────┘
            │
            ▼ (Threshold Grid Search 0.10 ~ 0.70)
  ┌────────────────────────────────────────────────────────────────────────┐
  │ 결과: LogLoss 0.4635 | ROC-AUC 0.6357 | Macro F1 0.5724 (+26.7% 폭등)   │
  │ 성과: 최적 임계값 0.22 발견 ➔ 완전히 놓치고 있던 지연편(TP) 13,039건 구출!│
  └────────────────────────────────────────────────────────────────────────┘
            │
            ▼
  [Phase 3: OOF Target Encoding] run_target_encoded.py
  ┌───────────────────────┐
  │ 고카디널리티 변수 변환│ ──▶ Tail_Number, Route 등을 과거 지연율로 치환
  └───────────────────────┘
            │
            ▼ (Data Leakage 없는 5-Fold 교차 타깃 인코딩 + 최적 임계값)
  ┌────────────────────────────────────────────────────────────────────────┐
  │ 결과: LogLoss 0.4635 | ROC-AUC 0.6043 | Macro F1 0.5521                │
  │ 교훈: 원본 범주형 변수를 삭제하여 트리 분기 능력 일부 저하 확인        │
  └────────────────────────────────────────────────────────────────────────┘
            │
            ▼
  [Phase 4: Hybrid Modeling] run_hybrid.py
  ┌───────────────────────┐
  │ 원본 카테고리 복원    │ ──▶ Category Dtype + OOF Target Encoding 동시 투입
  └───────────────────────┘
            │
            ▼ (트리 고유의 카테고리 분기력 + 수치형 지연 위험도 힌트 결합)
  ┌────────────────────────────────────────────────────────────────────────┐
  │ 결과: LogLoss 0.4633 | ROC-AUC 0.6107 | Macro F1 0.5579                │
  │ 성과: ROC-AUC 반등 및 순수 라벨 데이터 기준 LogLoss 최저치 경신        │
  └────────────────────────────────────────────────────────────────────────┘
            │
            ▼
  [Phase 5: Pseudo-Labeling] run_pseudo_labeling.py
  ┌───────────────────────┐
  │ 미라벨 74.5만 건 활용 │ ──▶ 4차 모델로 고확신 89,438건 선별하여 데이터 증강
  └───────────────────────┘
            │
            ▼ (비대칭 백분위수 필터링을 통한 준지도 학습 파이프라인)
  ┌────────────────────────────────────────────────────────────────────────┐
  │ 결과: LogLoss 0.4587 | ROC-AUC 0.6416 | Macro F1 0.5746                │
  │ 성과: 전 지표 역대 최고치(All-Time High) 달성 및 0.45대 LogLoss 진입   │
  └────────────────────────────────────────────────────────────────────────┘
            │
            ▼
  [Phase 6: Advanced Domain Features] run_advanced_features.py
  ┌───────────────────────┐
  │ 시간 결측 완전 복원   │ ──▶ 노선별 중앙값 소요시간 역산 + 시간대별 공항 혼잡도
  └───────────────────────┘
            │
            ▼ (순수 라벨 데이터 기반 도메인 피처링 검증)
  ┌────────────────────────────────────────────────────────────────────────┐
  │ 결과: LogLoss 0.4629 | ROC-AUC 0.6345 | Macro F1 0.5721                │
  │ 성과: 지연 감지(TP) 15,866건 달성 (Recall 35.26%로 역대 최다 적발)      │
  └────────────────────────────────────────────────────────────────────────┘
            │
            ▼
  [Phase 7: Weather Data Integration (실험 & 사후 부검)] run_phase7_weather_model.py
  ┌───────────────────────┐
  │ 외부 기상 데이터 결합 │ ──▶ 연도 역추적(2022년) + 미국 30대 허브 공항 시간별
  │                       │     Meteostat ASOS 실측치 결합 (전체 55.77% 매핑)
  └───────────────────────┘
            │
            ▼ (5-Fold 교차 검증 및 피처 중요도 분석)
  ┌────────────────────────────────────────────────────────────────────────┐
  │ 결과: LogLoss 0.4634 | ROC-AUC 0.5964 (급락) | Macro F1 0.5465         │
  │ 분석: 모든 기상 변수 Importance 0.0 기록 (트리가 완전 배제)            │
  │ 결론: 시차(UTC vs Local) 및 주최측 날짜 난독화/다년도 혼합으로 인한   │
  │       외부 데이터 정합성 왜곡 규명 ➔ 내부 도메인 피처 중심 복원 확정    │
  └────────────────────────────────────────────────────────────────────────┘
================================================================================================
```

---

## 4. EDA 및 피처 중요도 분석
* **최종 모델 피처 중요도 (Split 기준 Top 5):**
  1. `Route` (노선 결합, 51.7%): 특정 공항 간의 항로 병목 및 상공 혼잡도
  2. `Tail_Number` (기체 번호, 38.3%): 기체 회전에 따른 연쇄 지연(Reactionary Delay)
  3. `TE_Tail_Number` (기체 타깃 인코딩, 19.3%): 과거 지연 위험도 연속형 힌트
  4. `TE_Route` (노선 타깃 인코딩, 14.8%): 과거 노선별 지연 위험도 힌트
  5. `Sin_Dep_Hour` (출발 주기성, 7.1%): 심야/출근/피크 시간대 영향
* **기상 변수 중요도 (Phase 7 실측):**
  * `Weather_Origin_temp`: 0.2 (극소 미미)
  * `wspd`, `snow`, `prcp`, `wpgt`, `pres`, `coco`: **전부 0.0** (완전 배제 확인)

---

## 5. 모델링 및 성능 평가 (전 단계 실측치 비교)

### 5-1. 모델별 성능 지표 비교 (Stratified 5-Fold OOF 전수 실측치)

| 모델 단계 | 주요 실험 기법 | 최적 임계값 | Log Loss | Macro F1 | ROC-AUC | 지연 감지(TP) | Recall | 비고 |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: | :---: | :--- |
| **Phase 1: Baseline** | Label Encoding | 0.50 | 0.4636 | 0.4516 | 0.6366 | 0건 | 0.00% | 지연 예측 전멸 (All-Zero) |
| **Phase 2: Tuned** | Pruning + Threshold Search | 0.22 | 0.4635 | 0.5724 | 0.6357 | 13,039건 | 28.98% | 임계값 최적화로 F1 폭등 |
| **Phase 3: Target Encoded** | 범주형 제거 후 TE 단독 | 0.22 | 0.4635 | 0.5521 | 0.6043 | 14,895건 | 33.10% | 카테고리 분기력 상실로 AUC 하락 |
| **Phase 4: Hybrid** | 카테고리 + TE 동시 투입 | 0.22 | 0.4633 | 0.5579 | 0.6107 | 14,386건 | 31.97% | AUC 반등 및 LogLoss 개선 |
| **Phase 5: Pseudo-Labeling** | **8.9만 건 증강 준지도 학습** | **0.24** | **0.4587** | **0.5746** | **0.6416** | **13,527건** | **30.06%** | **전 지표 최고치 달성** |
| **Phase 6: Advanced Feature** | **시간 결측 역산 + 공항 혼잡도** | **0.21** | **0.4629** | **0.5721** | **0.6345** | **15,866건** | **35.26%** | **순수 라벨 기준 최다 지연 적발** |
| **Phase 7: Weather Integration** | **외부 기상 데이터 10종 결합** | 0.21 | 0.4634 | 0.5465 | 0.5964 | 16,857건 | 37.46% | **정합성 훼손으로 AUC 급락 (부검 완료)** |

---

## 6. 핵심 트러블슈팅 및 기술적 인사이트

### 6-1. 주요 문제 인식과 극복 과정
1. **불균형 데이터와 임계값의 함정:**
   * 자연 지연율이 17.65%인 상황에서 기본 임계값 0.50은 모델이 모든 예측을 0(정상)으로 판정하게 만듦.
   * 임계값을 자연 지연율에 근접한 **0.21~0.24로 조정하자 F1-Score가 0.4516 ➔ 0.5746으로 폭등**하며 실제 지연편을 안정적으로 검출.
2. **타깃 인코딩 시 원본 카테고리 보존의 중요성:**
   * 범주형 변수를 타깃 인코딩 실수값으로 완전 대체하면 LightGBM 고유의 카테고리 간 입체 분기 능력이 약화됨을 실측 검증.
   * 원본 카테고리를 유지한 채 타깃 인코딩 값을 '추가 피처'로 병합하는 **하이브리드 인코딩 방식**이 가장 안정적임을 확인.
3. **준지도 학습의 안전한 비대칭 필터링:**
   * 단순 백분위수 15:15 추출 시 정상 확률 78%짜리 비행기가 지연으로 라벨링되는 치명적 오염(Data Poisoning) 위험 발견.
   * **지연은 상위 2% 극단치만, 정상은 하위 10%만 추출하는 비대칭 필터링**을 통해 순도 높은 8.9만 건을 증강하여 LogLoss 0.4587 달성.
4. **단측 시간 결측치 역산 복원과 혼잡도 피처의 실효성:**
   * 결측된 비행시간을 동일 노선의 중앙값으로 역산 대치하고 공항 트래픽 피처를 추가한 결과, **지연 감지 건수(TP)가 15,866건(Recall 35.26%)으로 역대 최다 기록**을 달성함.
5. **[★ 핵심] Phase 7 사후 부검: 외부 데이터의 역설 (Paradox of External Data):**
   * **실험 의도**: 지연 원인의 70%를 차지하는 '기상 악화' 정보를 NOAA/Meteostat으로부터 2022년 미국 30대 공항 데이터로 결합하여 투입.
   * **실제 결과**: 기상 변수 전원의 피처 중요도가 0.0으로 찍히며, 전체 ROC-AUC가 0.64 ➔ 0.596으로 급락.
   * **실패 원인 규명**:
     1. **시공간 정합성(Alignment) 붕괴**: 주최 측의 날짜 셔플링/다년도 샘플링 난독화 가능성 및 UTC 표준시와 공항 현지 로컬 시각 간의 시차(5~8시간) 발생.
     2. **피처 서브샘플링 오염 (`colsample_bytree`)**: 트리가 생성될 때마다 75%의 변수를 무작위 선택하는데, 무의미한 10개 기상 노이즈 변수가 알짜 도메인 변수(`Route`, `Tail_Number`)의 학습 기회를 박탈하여 조기 종료(Early Stopping)를 유발함.
   * **공학적 교훈**: *"외부 데이터는 완벽한 시공간 키(Key) 정합성이 보장되지 않는다면, 모델에게 유용한 정보가 아니라 성능을 파괴하는 순수 노이즈가 된다."*

---

## 7. 최종 결론 및 권고 아키텍처

* **최종 결론**:
  * 외부 기상 데이터는 정합성 불일치 리스크로 인해 실전 배포에 부적합함이 통계적으로 검증됨.
  * 내부 도메인 역량을 극대화한 **Phase 5 (8.9만 건 고확신 준지도 증강) + Phase 6 (노선별 비행시간 역산 복원 & 공항 트래픽 혼잡도)** 파이프라인의 결합이 가장 신뢰도 높은 최적의 솔루션임.
* **최종 단일 모델 스펙 권고:**
  * 알고리즘: **LightGBM (GBDT)**
  * 범주형 처리: **Hybrid Encoding (Category Dtype + OOF Target Encoding)**
  * 데이터셋: **Phase 5 준지도 증강 데이터셋 (약 34.4만 행)**
  * 피처셋: **Phase 6 역산 소요시간 + 공항 혼잡도 + 주기성 삼각함수**
  * 분류 임계값: **0.22 (Macro F1 0.58+ 타깃 최적화)**
```